# Журнал верифікації та прогресу — Cem_copok

## Статус запуску
- **Локальний запуск**: Зупинено. Повністю пройдено бойовий тест для всіх трьох ботів.
- **Запуск у хмарі (Railway)**: Активний. Код та оновлені конфігурації успішно відправлено на GitHub. Стабілізовано та захищено від лімітів за допомогою автоматичного резервного копіювання на Groq API.
- **Стан юзербота (Клава)**: Авторизовано та успішно верифіковано в усіх трьох проектах через єдину робочу сесію.

## Лог подій та помилок
- **24.06.2026 10:45**: Впроваджено автоматичне резервне перемикання на Groq API (модель Llama-3.3) у разі помилок квоти Gemini (429/503).
  - *Проблема*: Всі три Gemini API ключі в `config.py` повністю вичерпали добовий ліміт запитів та почали повертати помилку `429 Quota Exceeded`. Через це генератор черги та автопостингу не міг створювати та публікувати контент (ШІ-пости не виходили 2 дні, психологія виходила частково, трейдинг пропускав новини та прокачку).
  - *Рішення*: Модернізовано клієнт `tools/gemini_client.py`. Якщо всі Gemini ключі повертають не-200 статуси, запит автоматично транслюється у формат OpenAI/Groq та надсилається на Groq API (`llama-3.3-70b-versatile`). Результат повертається у вигляді емульованого об'єкта `MockResponse`. Протестовано локально через `test_noon_generation.py`, оновлення завантажено на GitHub для автоматичного деплою на Railway.
- **19.06.2026 11:04**: Виправлено 4 критичні помилки за результатами повного аудиту:
  1. **Race condition о 14:00**: `PSY_SLOT_2_TIME` зсунуто з `14:00` → `13:30` (конфлікт з `noon_job` трейдингу).
  2. **Race condition о 19:00**: `PSY_SLOT_3_TIME` зсунуто з `19:00` → `19:30` (конфлікт з `daily_upgrade_job`).
  3. **Планувальник пропускав пости**: `time.sleep(30)` → `time.sleep(10)` у `schedule_thread_func` — при 30сек інтервалі задача могла не потрапити у вікно.
  4. **Неіснуючі моделі Gemini**: прибрано `gemini-3.5-flash` та `gemini-3.1-flash-lite` з `gemini_client.py` (завжди повертали 404). Актуальний список: `gemini-2.0-flash`, `gemini-2.5-flash`, `gemini-2.0-flash-lite`, `gemini-2.5-pro`.
  5. **Aiogram-боти розкоментовано**: Бот-Бібліотекар та Бот-Психолог тепер стартують на Railway. Контрольні перевірки зображень оновлено (PSY: 08:10 / 13:10 / 19:10).
- **17.06.2026 12:28**: Додано рубрики «Щоденна прокачка» (19:00) та «Фокус дня» (12:00) у канал @cem_copok, змінено ранковий огляд.
  - *Проблема*: Запит користувача на нові інформаційні та освітні рубрики зі збереженням лімітів зображень (через постійні забрендовані картинки) та видаленням BTC-рівнів і порад з ранкових оглядів.
  - *Рішення*:
    1. Повністю вилучено блоки підтримки/опору та порад у [poster.py](file:///f:/Antigravity/Cem_copok/tools/poster.py).
    2. Забрендовано та підготовлено постійні макети [daily_upgrade.jpg](file:///f:/Antigravity/Cem_copok/.tmp/daily_upgrade.jpg) та [focus_default.jpg](file:///f:/Antigravity/Cem_copok/.tmp/focus_default.jpg).
    3. Створено нові скрипти публікацій [daily_upgrader.py](file:///f:/Antigravity/Cem_copok/tools/daily_upgrader.py) та [focus_poster.py](file:///f:/Antigravity/Cem_copok/tools/focus_poster.py).
    4. Оновлено розклад у [main.py](file:///f:/Antigravity/Cem_copok/main.py) та [config.py](file:///f:/Antigravity/Cem_copok/config.py) для автоматичного запуску о 12:00 та 19:00.
- **14.06.2026 15:41**: Перенесено тижневий дайджест на 18:00 неділі, впроваджено лімітатор довжини підпису фото та розширено автокоментування Клави.
  - *Проблема*: Тижневий дайджест накладався на денні новини о 14:00. Також тижневий дайджест розбивався на два окремі повідомлення (фото + опис), якщо опис перевищував 1024 символи. Користувач попросив додати коментування та лайки у категоріях "Нерухомість" та "Манікюр".
  - *Рішення*:
    1. Змінено розклад `weekly_digest_job` в [main.py](file:///f:/Antigravity/Cem_copok/main.py) та [news_poster.py](file:///f:/Antigravity/Cem_copok/tools/news_poster.py) на 18:00 неділі.
    2. Реалізовано функцію `limit_caption_text` у [news_poster.py](file:///f:/Antigravity/Cem_copok/tools/news_poster.py), яка видаляє HTML-теги або обрізає опис до 1021 символу у разі перевищення ліміту в 1024 символи. Застосовано її до публікацій дайджесту та новин, щоб вони завжди надсилалися одним повідомленням.
    3. Створено нові конфігураційні змінні `REAL_ESTATE_CHANNELS` та `MANICURE_CHANNELS` у [config.py](file:///f:/Antigravity/Cem_copok/config.py) та додано генератори коментарів Gemini у [commenter.py](file:///f:/Antigravity/Cem_copok/tools/commenter.py).
- **08.06.2026 13:25**: Перенесено час денної публікації новин трейдингу на 14:00 та вимкнено репост Binance промо-акцій.
  - *Проблема*: 
    1. Денні огляди новин о 12:00 не виходили через таймаути та rate limit API Gemini на безкоштовних ключах. Також користувач попросив змінити час публікації на 14:00 для кращого розподілу контенту.
    2. Авторепости Binance промо-акцій засмічували стрічку спамом.
  - *Рішення*:
    1. Переписано клієнт `tools/gemini_client.py` для ротації 3 API-ключів та моделей.
    2. Зменшено RSS-ліміт до `limit=3` для зменшення промпту та усунення таймаутів.
    3. Змінено розклад та логування `noon_job` на 14:00 в [main.py](file:///f:/Antigravity/Cem_copok/main.py).
    4. Повністю відключено авторепости Binance промо-акцій (`TRADING_DONOR_CHANNELS = []` в `config.py`).
- **07.06.2026 17:28**: Вилучено промо-сигнатуру та кнопку голосового віджета з ранкового огляду.
  - *Проблема*: Запит користувача прибрати посилання на мікрофончик та супровідний промо-текст з ранкових оглядів.
  - *Рішення*: Вилучено додавання сигнатури `signature` та очищено `reply_markup` (встановлено `None` замість `InlineKeyboardMarkup`) у файлі [poster.py](file:///f:/Antigravity/Cem_copok/tools/poster.py).
- **07.06.2026 16:55**: Вилучено зайві тестові розклади публікацій.
  - *Проблема*: Залишалися активними тестові завдання публікацій о 14:15 для каналу трейдингу "Сім сорок" та о 14:20 для каналу "Психологія". Це призводило до небажаних подвійних постів щодня.
  - *Рішення*: Вилучено виклики `noon_job` о 14:15 та `psy_job_slot_1` о 14:20 у файлі [main.py](file:///f:/Antigravity/Cem_copok/main.py).
- **05.06.2026 14:25**: Виправлено таймаути Gemini API при публікації о 12:00 та помилку доступу репосту промо-акцій.
  - *Проблема*:
    1. Денний огляд новин трейдингу о 12:00 надсилав у Gemini API 45 новин, що призводило до таймаутів (45 сек) на Railway.
    2. Модель `gemini-2.0-flash` повертала помилку 429 через нульову квоту на використовуваних безкоштовних ключах.
    3. Авторепостер промо-акцій намагався публікувати повідомлення через юзербота, який не має прав адміністратора в каналі `@cem_copok`.
  - *Рішення*:
    1. Обмежено кількість новин у `fetch_rss_news` до 5 на одну стрічку (15 всього), що зменшило час генерації до ~25 сек.
    2. Вилучено `gemini-2.0-flash` з ротації моделей та додано `gemini-2.5-pro`.
    3. Переписано репост промо-акцій через Aiogram Bot API з токеном `BOT_TOKEN`.
    4. Зміни завантажено на GitHub для деплою на Railway.
- **05.06.2026 08:50**: Вирішено проблему відсутності ранкових публікацій та стабілізовано Gemini API.
  - *Проблема*:
    1. У коді `main.py` планувальника було упущено виклик `morning_job` о `config.MORNING_POST_TIME`.
    2. Нічна генерація контенту завершувалася збоями через таймаути та ліміти 429/503 Gemini API, ліміти платного ModelsLab та відсутність `huggingface_hub` на Railway.
  - *Рішення*:
    1. Ранковий пост успішно випущено вручну (`tools/poster.py`).
    2. Додано виклик `morning_job` у розклад `main.py`.
    3. Фірмову рамку `@cem_copok` накладено безпосередньо на статичний шаблон `.tmp/morning_default.png` на диску.
    4. Створено стійкий клієнт `tools/gemini_client.py` з ротацією моделей (`gemini-3.5-flash`, `gemini-2.5-flash`, `gemini-2.0-flash`, `gemini-flash-latest`) та експоненціальним бекоффом.
    5. Додано `huggingface_hub` у `requirements.txt` та змінено роздільну здатність картинок на 768x432 (16:9). Вимкнено ModelsLab.
    6. Всі зміни закомічено та надіслано в репозиторій GitHub для оновлення Railway.
- **04.06.2026 19:22**: Успішно проведено повний бойовий тест (`run_battle_test.py`).
  - *Рішення*:
    1. Для усунення помилок лімітів `429 Quota Exceeded` локальні конфігурації ботів переведені на власні виділені ключі Gemini API (`GEMINI_PSY_API_KEY` та `GEMINI_AI_API_KEY`).
    2. Встановлено пакет `huggingface_hub`, завдяки чому спрацював резервний механізм генерації через Hugging Face (FLUX) для бота ШІ.
    3. Пост ШІ успішно надіслано в телеграм-канал з унікальною згенерованою картинкою та накладеною брендовою рамкою/водяним знаком.
- **04.06.2026 18:38**: Вирішено блокування запуску на Railway.
  - *Проблема*: Запуск на Railway блокувався розподіленим блокуванням через активний локальний процес `Local-DESKTOP-SJRR82I`, який оновлював heartbeat.
  - *Рішення*: 
    1. Локальні процеси бота завершено примусово (`Stop-Process`).
    2. Статус локального блокування в Google Sheets (`BOT_LOCK`) оновлено на `STOPPED`.
    3. Виконано `git commit` та `git push` останніх змін сесії та коду в репозиторій на GitHub. Тепер контейнер на Railway має успішно запуститися.
- **04.06.2026 18:08**: Виявлено критичний збій `AuthKeyDuplicatedError`.
  - *Причина*: Одночасне підключення з одного сесійного файлу `klava.session` локально та з сервера Railway. Telegram анулював ключ.
  - *Рішення*:
    1. Очищено таблиці `sessions` та `update_state` у `klava.session` (без видалення самого файлу для збереження кешу відповідно до Rule 0).
    2. Створено та запущено допоміжний скрипт `login_qr.py` для авторизації через QR-код.
    3. Користувач успішно відсканував QR-код через додаток Telegram. Юзербот `k_l_a_w_a` авторизований.
    4. Новий ключ успішно записано у `klava.session`.

## Попередження щодо безпеки
> [!WARNING]
> Щоб уникнути повторного виникнення `AuthKeyDuplicatedError` та анулювання сесії, **ніколи** не запускайте бот локально, коли запущено деплой на Railway з цим же файлом `klava.session`. Перед локальним тестуванням обов'язково зупиняйте (Pause) сервіс на Railway.

