# Журнал верифікації та прогресу — Cem_copok

## Статус запуску
- **Локальний запуск**: Зупинено. Повністю пройдено бойовий тест для всіх трьох ботів.
- **Запуск у хмарі (Railway)**: Активний. Код та оновлені конфігурації успішно відправлено на GitHub. Інстанс Railway оновив розклад (Психологія о 08:30, ШІ о 09:00).
- **Стан юзербота (Клава)**: Авторизовано та успішно верифіковано в усіх трьох проектах через єдину робочу сесію.

## Лог подій та помилок
- **07.06.2026 16:55**: Вилучено зайві тестові розклади публікацій.
  - *Проблема*: Залишалися активними тестові завдання публікацій о 14:15 для каналу трейдингу "Сім сорок" та о 14:20 для каналу "Психологія". Це призводило до небажаних подвійних постів щодня.
  - *Рішення*: Вилучено виклики `noon_job` о 14:15 та `psy_job_slot_1` о 14:20 у файлі [main.py](file:///f:/Antigravity/Cem_copok/main.py).
- **05.06.2026 14:25**: Виправлено таймаути Gemini API при публікації о 12:00 та помилку доступу репосту промо-акцій.
  - *Проблема*:
    1. Денний огляд новин трейдингу о 12:00 надсилав у Gemini API 45 новин, що призводило до таймаутів (45 сек) на Railway.
    2. Модель `gemini-2.0-flash` повертала помилку 429 через нульову квоту на використовуваних безкоштовних ключах.
    3. Авторепостер промо-акцій намагався публікувати повідомлення через юзербота, який не має прав адміністратора в каналі `@cem_copok`.
  - *Рішення*:
    1. Обмежено кількість новин у `fetch_rss_news` до 5 на одну стрічку (15 всього), що зменшило час генерації до ~25 сек.
    2. Вилучено `gemini-2.0-flash` з ротації моделей та додано `gemini-2.5-pro`.
    3. Переписано репост промо-акцій через Aiogram Bot API з токеном `BOT_TOKEN`.
    4. Зміни завантажено на GitHub для деплою на Railway.
- **05.06.2026 08:50**: Вирішено проблему відсутності ранкових публікацій та стабілізовано Gemini API.
  - *Проблема*:
    1. У коді `main.py` планувальника було упущено виклик `morning_job` о `config.MORNING_POST_TIME`.
    2. Нічна генерація контенту завершувалася збоями через таймаути та ліміти 429/503 Gemini API, ліміти платного ModelsLab та відсутність `huggingface_hub` на Railway.
  - *Рішення*:
    1. Ранковий пост успішно випущено вручну (`tools/poster.py`).
    2. Додано виклик `morning_job` у розклад `main.py`.
    3. Фірмову рамку `@cem_copok` накладено безпосередньо на статичний шаблон `.tmp/morning_default.png` на диску.
    4. Створено стійкий клієнт `tools/gemini_client.py` з ротацією моделей (`gemini-3.5-flash`, `gemini-2.5-flash`, `gemini-2.0-flash`, `gemini-flash-latest`) та експоненціальним бекоффом.
    5. Додано `huggingface_hub` у `requirements.txt` та змінено роздільну здатність картинок на 768x432 (16:9). Вимкнено ModelsLab.
    6. Всі зміни закомічено та надіслано в репозиторій GitHub для оновлення Railway.
- **04.06.2026 19:22**: Успішно проведено повний бойовий тест (`run_battle_test.py`).
  - *Рішення*:
    1. Для усунення помилок лімітів `429 Quota Exceeded` локальні конфігурації ботів переведені на власні виділені ключі Gemini API (`GEMINI_PSY_API_KEY` та `GEMINI_AI_API_KEY`).
    2. Встановлено пакет `huggingface_hub`, завдяки чому спрацював резервний механізм генерації через Hugging Face (FLUX) для бота ШІ.
    3. Пост ШІ успішно надіслано в телеграм-канал з унікальною згенерованою картинкою та накладеною брендовою рамкою/водяним знаком.
- **04.06.2026 18:38**: Вирішено блокування запуску на Railway.
  - *Проблема*: Запуск на Railway блокувався розподіленим блокуванням через активний локальний процес `Local-DESKTOP-SJRR82I`, який оновлював heartbeat.
  - *Рішення*: 
    1. Локальні процеси бота завершено примусово (`Stop-Process`).
    2. Статус локального блокування в Google Sheets (`BOT_LOCK`) оновлено на `STOPPED`.
    3. Виконано `git commit` та `git push` останніх змін сесії та коду в репозиторій на GitHub. Тепер контейнер на Railway має успішно запуститися.
- **04.06.2026 18:08**: Виявлено критичний збій `AuthKeyDuplicatedError`.
  - *Причина*: Одночасне підключення з одного сесійного файлу `klava.session` локально та з сервера Railway. Telegram анулював ключ.
  - *Рішення*:
    1. Очищено таблиці `sessions` та `update_state` у `klava.session` (без видалення самого файлу для збереження кешу відповідно до Rule 0).
    2. Створено та запущено допоміжний скрипт `login_qr.py` для авторизації через QR-код.
    3. Користувач успішно відсканував QR-код через додаток Telegram. Юзербот `k_l_a_w_a` авторизований.
    4. Новий ключ успішно записано у `klava.session`.

## Попередження щодо безпеки
> [!WARNING]
> Щоб уникнути повторного виникнення `AuthKeyDuplicatedError` та анулювання сесії, **ніколи** не запускайте бот локально, коли запущено деплой на Railway з цим же файлом `klava.session`. Перед локальним тестуванням обов'язково зупиняйте (Pause) сервіс на Railway.

